import React from "react";
import {
  Dialog,
  DialogContent,
  DialogTrigger,
} from "~/modules/shad/components/ui/dialog";

const CursedPossessionsWrapper = (): JSX.Element => {
  return <div className="-mb-4 flex flex-col gap-2 py-2"></div>;
};

export default CursedPossessionsWrapper;
